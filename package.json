{
  "name": "github-gateway",
  "version": "1.0.0",
  "description": "A single-page application which allows users to search for a GitHub profile by username.",
  "main": "github-gateway/src/server.ts",
  "scripts": {
    "dev": "npx ng serve",
    "build": "npx ng build",
    "start:prod": "node dist/github-search/server/server.mjs",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "vitest run src/server/tests",
    "test:frontend": "npx ng test --watch=false"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bootstrap": "^5.3.8",
    "express": "^5.2.1",
    "mongodb": "^7.5.0",
    "mongoose": "^9.7.4"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
