{
  "name": "github-gateway",
  "version": "1.0.0",
  "description": "A single-page application which allows users to search for a GitHub profile by username.",
  "main": "github-gateway/src/server.ts",
  "scripts": {
    "dev": "npm run dev --prefix github-gateway",
    "build": "npm run build --prefix github-gateway",
    "start:prod": "npm run start:prod --prefix github-gateway",
    "test": "npm run test:backend --prefix github-gateway && npm run test:frontend --prefix github-gateway",
    "test:backend": "npm run test:backend --prefix github-gateway",
    "test:frontend": "npm run test:frontend --prefix github-gateway"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bootstrap": "^5.3.8",
    "express": "^5.2.1",
    "mongodb": "^7.5.0",
    "mongoose": "^9.7.4"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
